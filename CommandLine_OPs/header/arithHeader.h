#ifndef __ARITHHEADER_H__

#define __ARITHHEADER_H__

#include <stdio.h>
#include <string.h>
#include <stdlib.h> //atoi(), strtol()
#include <errno.h>

void usage();
int Addition(const int, char** );
int Subtraction(const int, char** );
int Multiplication(const int, char** );
int Division(const int, char** );


#endif

